The build used to take forty minutes. Now it takes six.

We cut three steps from the pipeline and cached the dependency layer. That was it — no rewrite, no new tooling. The next morning, the team shipped twice before lunch.
